---
workloads:
  - name: userapi
    intercepts:
      - handler: userapi
        port: 80
        localPort: 8080
        headers:
          - name: "test-{{ .Telepresence.Username }}"
            value: "1"
handlers:
  - name: userapi
    environment:
      - name: DATASET
        value: remote
    docker:
      ports:
        - 8080
      compose:
        context: "."
        services:
          - name: userapi
            behavior: interceptHandler

          - name: contactapi
            behavior: remote

          - name: postgres
            behavior: remote
...
